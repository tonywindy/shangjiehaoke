# Cloudflare Pages automatically maps built HTML files such as
# /works.html to their extensionless canonical URLs (/works).
# Keep only the intentional legacy alias below.
/quotes         /contact       301
